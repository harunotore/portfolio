export interface PostCardType {
    slug: string
    meta: metaType
}

export interface metaType {
    date: string
    thumbnail: string
    title: string
    description: string
    readTime: string
}